# Reality Simulator Dependencies

# Core Scientific Computing (REQUIRED)
numpy>=2.0.0
scipy>=1.14.0

# Network/Graph Analysis (REQUIRED)
networkx>=3.0

# System Monitoring (REQUIRED)
psutil>=5.9.0

# Visualization (RECOMMENDED)
matplotlib>=3.8.0   # For visualization plots and composite image generation

# Web UI (RECOMMENDED)
flask>=3.0.0        # For Causation Explorer web interface
flask-socketio>=5.3.0  # For CRA real-time streaming support
gunicorn>=21.0.0    # For production WSGI server (Render deployment)
requests>=2.31.0    # For Ollama API communication (cloud and local)
Pillow>=10.0.0      # For image compression in vision model analysis

# Explorer (Windows only)
pywin32>=306; sys_platform == 'win32'  # For Explorer process isolation (Windows only)

# Neural & ML Systems (OPTIONAL - system works without these)
torch>=2.5.0        # Core Neural Engine - DQN reinforcement learning for organisms
scikit-learn>=1.5.0 # ML utilities - clustering, anomaly detection, PCA/t-SNE visualization
hdbscan>=0.8.38     # HDBSCAN density-based clustering for phenotype detection
onnxruntime>=1.15.0 # For running exported ONNX models (agent compilation)

# Distributed Computing (OPTIONAL - graceful fallback to sequential)
ray>=2.10.0         # Parallel processing for ML features, battles, neural decisions, training (2-5x speedup)

# Kernel Dependencies (required if using Djinn Kernel features)
cryptography>=42.0.0  # Used in kernel/security_compliance.py for security features

# External (not installed via pip here): FFmpeg required for video export (see README)

# Note: Neural system is optional. If PyTorch is not installed, organisms use standard genetic evolution only.
# The simulator runs on pure evolution, network dynamics, and quantum physics, with optional neural learning.

