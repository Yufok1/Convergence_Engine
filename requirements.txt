# Reality Simulator Dependencies

# Core Scientific Computing (REQUIRED)
# NOTE: numpy<2.0 required for PyFlyt drone support
numpy>=1.24.0,<2.0
scipy>=1.11.0,<1.14      # scipy 1.14+ wants numpy 2.x

# Network/Graph Analysis (REQUIRED)
networkx>=3.0,<4.0

# System Monitoring (REQUIRED)
psutil>=5.9.0,<6.0

# Visualization (RECOMMENDED)
matplotlib>=3.9.0,<3.10   # Pin to 3.9.x for stability

# Web UI (RECOMMENDED)
flask>=3.0.0,<4.0        # For Causation Explorer web interface
flask-socketio>=5.3.0,<6.0  # For CRA real-time streaming support
gunicorn>=21.0.0,<24.0    # For production WSGI server (Render deployment)
requests>=2.31.0,<3.0    # For Ollama API communication (cloud and local)
Pillow>=10.0.0,<13.0      # For image compression in vision model analysis

# CLI Dashboard (RECOMMENDED)
rich>=13.0.0,<14.0       # For live_dashboard.py interactive terminal UI

# Explorer (Windows only)
pywin32>=306; sys_platform == 'win32'  # For Explorer process isolation (Windows only)

# Neural & ML Systems (OPTIONAL - system works without these)
# NOTE: Default torch installs CPU-only. For GPU, use requirements-cuda.txt or:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
hdbscan==0.8.40          # Pin exact - fights with scikit-learn
torch>=2.5.0,<3.0        # Core Neural Engine - DQN reinforcement learning for organisms (CPU by default!)
scikit-learn>=1.5.0,<1.6.0 # Pin range - hdbscan compat
onnx>=1.16.0,<1.18  # ONNX format - pinned for onnxscript compatibility
onnxscript>=0.1.0,<0.2  # Required by PyTorch 2.5+ for ONNX export - pin to avoid onnx_ir conflicts
onnxruntime>=1.17.0,<1.20 # For running exported ONNX models - pinned for compatibility

# GPU Acceleration (OPTIONAL - for torch.compile with Triton backend)
# NOTE: Official triton only supports Linux. For Windows, use triton-windows community build:
#   pip install triton-windows
# triton>=3.0.0     # Linux only - Triton GPU compiler for torch.compile (2-3x speedup)


# Gaming Environments (OPTIONAL - for cocoon exported agents)
gymnasium==0.29.1        # Pin exact - PyFlyt 0.29 needs this
# gymnasium[mujoco]      # DISABLED - causes dep hell, install separately if needed
# box2d-py>=2.3.8        # DISABLED - requires swig: apt-get install swig build-essential
pygame>=2.5.0,<3.0       # PyGame for PLE games rendering
# tmrl>=0.7.0       # TrackMania RL - see cocoon_tmrl_adapter.py (pip install tmrl)
# ple                 # PyGame Learning Environment (pip install ple or git install)
# ale-py>=0.8.1       # Atari Learning Environment for Atari games (large download)

# Drone Warfare Arena (8 game modes with NASA JSBSim 6-DOF physics)
# matplotlib is already included above for visualization
PyFlyt==0.29.0        # Pin exact - requires gymnasium 0.29.x

# P2P Networking (OPTIONAL - for cocoon link mode)
websockets>=12.0,<16.0    # WebSocket client/server for CocoonHatch P2P battles

# Language & Vocabulary (REQUIRED for curated vocabulary pipeline)
nltk>=3.8.1,<4.0         # WordNet lexical database for zero-proper-noun vocabulary (120k+ words)

# Distributed Computing (DISABLED - Ray metrics agent causes hangs)
# ray>=2.10.0       # DISABLED - metrics agent spam blocks startup for 10+ minutes

# Kernel Dependencies (required if using Djinn Kernel features)
cryptography>=42.0.0,<47.0  # Used in kernel/security_compliance.py for security features

# External (not installed via pip here): FFmpeg required for video export (see README)

# Note: Neural system is optional. If PyTorch is not installed, organisms use standard genetic evolution only.
# The simulator runs on pure evolution, network dynamics, and quantum physics, with optional neural learning.

