# CUDA-enabled PyTorch for GPU acceleration
# 
# USE THIS for RunPod, Colab, AWS, or any NVIDIA GPU environment
# The default requirements.txt installs CPU-only PyTorch
#
# Installation:
#   pip install -r requirements-cuda.txt
#
# Or manually install PyTorch with CUDA:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#
# Check your CUDA version first:
#   nvidia-smi | grep "CUDA Version"
#
# CUDA 12.1 (most common):
--extra-index-url https://download.pytorch.org/whl/cu121
torch>=2.5.0
torchvision>=0.20.0
torchaudio>=2.5.0

# Then install the rest of the dependencies
-r requirements.txt
